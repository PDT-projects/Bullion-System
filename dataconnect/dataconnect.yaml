specVersion: "v1"
serviceId: "erp-system-service"
location: "asia-south1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "erp-system-database"
      cloudSql:
        instanceId: "erp-system-instance"
connectorDirs: ["./example", "./banking", "./loans", "./budgets", "./inventory"]
